# =============================================================================
# Twines and Straps SA - Environment Variables
# =============================================================================
# This file contains all required environment variables for the application.
# Copy this file to .env and fill in your actual values.
# NEVER commit your .env file with real secrets to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# The project uses PostgreSQL by default (compatible with Neon and Supabase).
#
# ⚠️  CRITICAL: SQLite file-based databases (file:./dev.db) DO NOT work in
#     serverless environments like Netlify. The database file is not persisted
#     between serverless function invocations.
#
# RECOMMENDED PROVIDERS (all have free tiers):
# - Neon (Postgres): https://neon.tech/ - **Recommended**
# - Supabase (Postgres): https://supabase.com/
# - Turso (SQLite-compatible): https://turso.tech/
#
# Example connection strings:
# DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"
# DATABASE_URL="postgresql://user:password@host.supabase.co:5432/postgres"
#
# For Turso, you need to change the provider in prisma/schema.prisma to "sqlite"
# and use the libsql adapter:
# DATABASE_URL="libsql://your-db.turso.io?authToken=your-token"
#
# How to get your DATABASE_URL:
# 1. Sign up for Neon (https://neon.tech/) or Supabase (https://supabase.com/)
# 2. Create a new database/project
# 3. Copy the connection string from the dashboard
# 4. For Netlify: Add this in Site Settings → Environment Variables
#
# For LOCAL DEVELOPMENT with SQLite (requires changing prisma/schema.prisma provider):
# DATABASE_URL="file:./dev.db"
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"

# -----------------------------------------------------------------------------
# WHATSAPP BUSINESS CONFIGURATION
# -----------------------------------------------------------------------------
# Your WhatsApp Business number for quote requests
# Format: Country code + number (no + or spaces)
# Example: 27821234567 (South Africa)
#
# How to set up:
# 1. Download WhatsApp Business from app store
# 2. Register with your business phone number
# 3. Enter the number below in format: [country code][number]
NEXT_PUBLIC_WHATSAPP_NUMBER="27XXXXXXXXX"

# -----------------------------------------------------------------------------
# AZURE AI FOUNDRY CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Azure AI Foundry provides AI capabilities including image generation (DALL-E).
# These are optional and only needed if using AI-generated product images.
#
# How to get your Azure AI credentials:
# 1. Go to Azure Portal: https://portal.azure.com/
# 2. Create an Azure account if you don't have one
# 3. Navigate to "Azure AI Foundry" (formerly Azure OpenAI Service)
#    - Search for "Azure AI Foundry" in the portal
#    - Or visit: https://ai.azure.com/
# 4. Create a new resource/project:
#    - Click "Create a resource"
#    - Select your subscription
#    - Create or select a resource group
#    - Choose a region (e.g., East US, West Europe)
#    - Enter a unique name for your resource
# 5. Once created, go to "Keys and Endpoint" in your resource:
#    - Copy "KEY 1" or "KEY 2" for AZURE_AI_API_KEY
#    - Copy "Endpoint" for AZURE_AI_ENDPOINT
# 6. Deploy a model:
#    - Go to "Model deployments" → "Deploy model"
#    - Select DALL-E 3 for image generation
#    - Copy the deployment name for AZURE_AI_DEPLOYMENT_NAME
#
# For detailed setup guide, see:
# https://learn.microsoft.com/en-us/azure/ai-services/openai/quickstart
AZURE_AI_ENDPOINT="https://your-resource-name.openai.azure.com/"
AZURE_AI_API_KEY="your-azure-ai-api-key"
AZURE_AI_DEPLOYMENT_NAME="dall-e-3"

# -----------------------------------------------------------------------------
# NETLIFY DEPLOYMENT (CI/CD)
# -----------------------------------------------------------------------------
# These are used for GitHub Actions deployment to Netlify.
# Set these as GitHub repository secrets (Settings → Secrets and variables → Actions)
#
# How to get Netlify credentials:
# 1. Log in to Netlify: https://app.netlify.com/
# 2. For NETLIFY_SITE_ID:
#    - Go to your site dashboard
#    - Site Settings → General → Site details
#    - Copy the "Site ID" (also called API ID)
# 3. For NETLIFY_DEPLOY_TOKEN:
#    - Go to User Settings (click your avatar → User settings)
#    - Applications → Personal access tokens
#    - Click "New access token"
#    - Give it a descriptive name (e.g., "GitHub Actions Deploy")
#    - Copy the token immediately (it won't be shown again)
#
# Add these as GitHub secrets, NOT in your .env file:
# NETLIFY_SITE_ID="your-netlify-site-id"
# NETLIFY_DEPLOY_TOKEN="your-netlify-personal-access-token"
